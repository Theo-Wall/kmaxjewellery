import { Box, Typography } from "@mui/material";

const Home = () => {
  return (
    <Box>
      <Typography sx={{ color: "black", border: "1px solid pink" }}>
        hello
      </Typography>
    </Box>
  );
};

export default Home;
